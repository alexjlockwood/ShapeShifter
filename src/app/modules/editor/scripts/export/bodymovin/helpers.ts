export enum BlendMode {
  Normal = 0,
  Multiply,
  Screen,
  Overlay,
  Darken,
  Lighten,
  ColorDodge,
  ColorBurn,
  HardLight,
  SoftLight,
  Difference,
  Exclusion,
  Hue,
  Saturation,
  Color,
  Luminosity,
}

export enum Boolean {
  False = 0,
  True,
}

export enum FillRule {
  NonZero = 1,
  EvenOdd,
}

export enum LineCap {
  Butt = 1,
  Round,
  Square,
}

export enum LineJoin {
  Miter = 1,
  Round,
  Bevel,
}
