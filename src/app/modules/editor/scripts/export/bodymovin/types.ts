// TODO: ask Hernan if this is the correct definition of a bodymovin color (specifically the last 'alpha' number)
/** An RGBA color. Each number is in the interval [0,1]. */
export type Color = [number, number, number, number];
