// TODO: ask Hernan if we can support translucent alpha values

/** An RGBA color. First 3 numbers are in the interval [0,1]. Last number is always 1. */
export type Color = [number, number, number, 1];

/** A 3D point. */
export type Point = [number, number, number];
