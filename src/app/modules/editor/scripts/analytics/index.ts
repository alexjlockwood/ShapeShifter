/**
 * Records a user interaction. This is currently a no-op: the Universal Analytics property the
 * app used to report to no longer accepts data.
 */
export function trackEvent(category: string, action: string, label?: string) {}
