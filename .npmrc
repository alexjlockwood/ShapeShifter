omit-lockfile-registry-resolved=true
# Only install versions that have been published for at least 5 days, to give the registry time
# to pull compromised releases. npm only supports this from 11.10, and older versions silently
# ignore it, so engine-strict makes them refuse to install instead (see engines in package.json).
min-release-age=5
engine-strict=true
